---
import { getMonthlyArchives } from '../utils/archives';

const archives = await getMonthlyArchives();
---

<nav>
  <h2>Archives</h2>
  <ul>
    {
      archives.map(({ yearMonth, label }) => {
        const href = `/${yearMonth}`;

        return (
          <li>
            <a href={href}>{label}</a>
          </li>
        );
      })
    }
  </ul>
</nav>

<style>
  ul {
    list-style: none;
    padding: 0;
  }

  li {
    margin-bottom: 0.5rem;
  }

  li:last-child {
    margin-bottom: 0;
  }

  a {
    color: inherit;
    text-decoration: none;
  }
</style>
